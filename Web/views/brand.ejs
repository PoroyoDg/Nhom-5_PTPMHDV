<%- include ('header')%>
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Brand</h1>

          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <div class="row">
      <div class="col-12">
        <div class="card" style="height: 480px;">
          <div class="card-header">

            <form action="" method="get" >
              <h3 class="card-title">
                Brand list
              </h3>

              <div class="card-tools">
                <div class="input-group input-group-sm" style="text-align: right;" style="width: 150px;">
                  <input name="name" class="form-control float-right" placeholder="Search..">
                  <div class="input-group-append">
                    <button type="submit" class="btn btn-default"><i class="fas fa-search"></i></button>
                  </div>
                </div>
              </div>
            </form>

          </div>
          <!-- /.card-header -->
          <div class="card-body table-responsive p-0" style="height: 300px;">
            <table class="table table-head-fixed">
              <thead>
                <tr>
                  <th>STT</th>
                  <th>Name</th>
                  <th>OS</th>
                  <th><a href="/addBrand" class="btn btn-success"><i class="fa fa-plus"></i> Add Brand</a></th>
                </tr>
              </thead>
              <tbody>
                <% data.map(function(br,index){ %>
                  <tr>
                    <td>
                      <%= index + 1 %>
                    </td>
                    <td>
                      <%= br.name %>
                    </td>
                    <td>
                      <%= br.os %>
                    </td>
                    
                    <td>
                      <a href="/editBrand/<%= br.id %>" class="btn btn-primary"><i class="fa fa-edit"></i> Edit</a>
                      <a href="/deleteBrand/<%= br.id %>" class="btn btn-danger"><i class="fa fa-trash"></i>
                        Delete</a>
                        
                    </td>
                  </tr>
                  <% }) %>

              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
        <!-- /.card -->
      </div>
    </div>
    <!-- /.row -->
  </div>
  <%- include ('footer')%>